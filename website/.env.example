# Placeholders for every environment variable docker-compose.yml references
# for the web-app service. No real secrets - copy to .env and fill in real
# local values.

WEB_APP_PORT=5173
WEB_APP_API_URL=http://localhost:8000
GOOGLE_MAPS_API_KEY=change_me
WEB_APP_OAUTH_CLIENT_ID=change_me
