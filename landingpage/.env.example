# Placeholders for every environment variable docker-compose.yml references
# for the landing-page-plans service. No real secrets - copy to .env and
# fill in real local values.

LANDING_PAGE_PLANS_PORT=5175
LANDING_PAGE_PLANS_API_URL=http://localhost:8000
